# =============================================================================
# User Service — environment template
# Docker: postgres, rabbitmq, auth-service hostnames
# Local:  localhost:5432, localhost:5672, localhost:50051
# =============================================================================

NODE_ENV=development
PORT=3000

# PostgreSQL (database: collabspace_user)
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/collabspace_user?schema=public
DATABASE_SCHEMA=public
DATABASE_SSL=false
DATABASE_SYNCHRONIZE=false
DATABASE_LOGGING=false

# RabbitMQ (consume auth events; publish user_registered / user_profile_updated)
RABBITMQ_ENABLED=true
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/collabspace
RABBITMQ_QUEUE=user-service
RABBITMQ_QUEUE_DURABLE=true
RABBITMQ_PREFETCH_COUNT=10
RABBITMQ_NO_ACK=false

# gRPC server (profiles for auth-service)
GRPC_ENABLED=true
GRPC_URL=0.0.0.0:50052

# Auth gRPC client (verify JWT on HTTP routes)
AUTH_SERVICE_GRPC_URL=auth-service:50051
AUTH_SERVICE_GRPC_TIMEOUT_MS=3000

TRACING_ENABLED=false
OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318/v1/traces
JAEGER_AGENT_HOST=jaeger
OTEL_LOG_LEVEL=

# Metrics scrape lockdown (optional)
METRICS_AUTH_TOKEN=
