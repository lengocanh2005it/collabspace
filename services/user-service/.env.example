# =============================================================================
# User Service — environment template
# Copy to .env (cp .env.example .env). Đừng commit .env.
#
# Bootstrap secrets — chọn một:
#
# A) Vault (khuyến nghị — field VAULT_SYNC / để trống được sync tự động):
#    cd infrastructure/docker && docker compose -f docker-compose.vault.yml up -d
#    cd ../.. && .\infrastructure\vault\scripts\seed-dev-secrets.ps1
#    .\infrastructure\vault\scripts\sync-env-from-vault.ps1
#
# B) Điền tay — khớp infrastructure/docker/.env.example + vault/.env.example
#
# Docker Compose: postgres, rabbitmq, auth-service hostnames
# Local pnpm:     localhost:5432, localhost:5672, AUTH_SERVICE_GRPC_URL=127.0.0.1:50051
# =============================================================================

NODE_ENV=development
PORT=3000

# -- PostgreSQL ----------------------------------------------------------------
# Password trong URL là VAULT-MANAGED
DATABASE_URL=postgresql://postgres:VAULT_SYNC@postgres:5432/collabspace_user?schema=public
DATABASE_SCHEMA=public
DATABASE_SSL=false
DATABASE_SYNCHRONIZE=false
DATABASE_LOGGING=false

# -- RabbitMQ (consumer: auth events → create pending profile) -----------------
# Credentials trong URL là VAULT-MANAGED
RABBITMQ_ENABLED=true
RABBITMQ_URL=amqp://user:VAULT_SYNC@rabbitmq:5672/collabspace

# -- User outbox (Phase 4 — CDC → Kafka) ---------------------------------------
# debezium = không publish user_registered / user_profile_updated qua RabbitMQ
# rabbitmq = dual-run (outbox + RMQ sau commit, prod rollback)
USER_OUTBOX_PUBLISH_MODE=debezium

# -- gRPC server ---------------------------------------------------------------
GRPC_ENABLED=true
GRPC_URL=0.0.0.0:50052

# -- Auth gRPC -----------------------------------------------------------------
AUTH_SERVICE_GRPC_URL=auth-service:50051
AUTH_SERVICE_GRPC_TIMEOUT_MS=3000
AUTH_SERVICE_HTTP_URL=http://auth-service:3000
AUTH_SERVICE_HTTP_TIMEOUT_MS=3000

# -- Service-to-service HTTP (Service JWT) ------------------------------------
SERVICE_JWT_SECRET=                  # VAULT-MANAGED — sync script điền; khớp user/workspace/task/notification

# -- Tracing -------------------------------------------------------------------
TRACING_ENABLED=false
OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318/v1/traces
JAEGER_AGENT_HOST=jaeger
OTEL_LOG_LEVEL=

# -- Metrics -------------------------------------------------------------------
METRICS_AUTH_TOKEN=                  # VAULT-MANAGED (để trống = không yêu cầu auth ở local)

# -- Azure Blob (avatar upload) -----------------------------------------------
# Optional — không set = mock URL (ui-avatars.com), không persist blob
# AZURE_STORAGE_CONNECTION_STRING=   # VAULT-MANAGED (prod)
