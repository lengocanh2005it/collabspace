# =============================================================================
# Auth Service — environment template
# Copy to .env (cp .env.example .env). Đừng commit .env.
#
# Bootstrap secrets — chọn một:
#
# A) Vault (khuyến nghị — field VAULT_SYNC / để trống được sync tự động):
#    cd infrastructure/docker && docker compose -f docker-compose.vault.yml up -d
#    cd ../.. && .\infrastructure\vault\scripts\seed-dev-secrets.ps1
#    .\infrastructure\vault\scripts\sync-env-from-vault.ps1
#
# B) Điền tay — khớp infrastructure/docker/.env.example + vault/.env.example
#    postgres/postgres, guest/guest, collabspace123, collabspace-dev-jwt-* ...
#
# Docker Compose: postgres, redis, user-service hostnames
# Local pnpm:     localhost + mapped ports (5432, 6379, 50052, ...)
# =============================================================================

NODE_ENV=development
PORT=3000

# -- PostgreSQL ----------------------------------------------------------------
# Password trong URL là VAULT-MANAGED — sync script sẽ điền đúng giá trị
DATABASE_URL=postgresql://postgres:VAULT_SYNC@postgres:5432/collabspace_auth?schema=public
DATABASE_SCHEMA=public
DATABASE_SSL=false
DATABASE_SYNCHRONIZE=false
DATABASE_LOGGING=false
DATABASE_AUTO_LOAD_ENTITIES=true

# -- JWT -----------------------------------------------------------------------
JWT_SECRET=                          # VAULT-MANAGED
JWT_EXPIRY=24h
JWT_ISSUER=
JWT_AUDIENCE=

# -- Service-to-Service JWT (inbound S2S token verification) -------------------
# Required: auth-service verifies SERVICE_JWT tokens from workspace-service.
# Must match SERVICE_JWT_SECRET across all services in the same environment.
SERVICE_JWT_SECRET=                  # VAULT-MANAGED — same value as other services

# -- Verify lite cache (downstream AuthGuard hot path) ---------------------------
AUTH_VERIFY_LITE_CACHE_ENABLED=true
AUTH_VERIFY_LITE_CACHE_MAX_TTL_SECONDS=300

# -- Email verification / password reset ---------------------------------------
EMAIL_VERIFICATION_OTP_LENGTH=6
EMAIL_VERIFICATION_OTP_TTL_SECONDS=600
EMAIL_VERIFICATION_RESEND_COOLDOWN_SECONDS=60
EMAIL_VERIFICATION_RESEND_MAX_ATTEMPTS=5
EMAIL_VERIFICATION_RESEND_WINDOW_SECONDS=3600
PASSWORD_RESET_TOKEN_BYTE_LENGTH=32
PASSWORD_RESET_TTL_SECONDS=1800

# -- Refresh tokens ------------------------------------------------------------
REFRESH_TOKEN_TTL_DAYS=30
REFRESH_TOKEN_BYTE_LENGTH=48

# -- User service gRPC ---------------------------------------------------------
USER_SERVICE_GRPC_URL=user-service:50052
USER_SERVICE_GRPC_TIMEOUT_MS=3000

# -- Brevo transactional email (OTP xác thực & reset password) -----------------
# API key: Brevo dashboard → SMTP & API → API keys (xkeysib-...)
# Sender email phải được verify trong Brevo.
BREVO_API_KEY=
BREVO_SENDER_EMAIL=
BREVO_SENDER_NAME="CollabSpace Platform"
EMAIL_DELIVERY_TIMEOUT_MS=15000
EMAIL_QUEUE_TIMEOUT_MS=5000
EMAIL_JOB_MAX_ATTEMPTS=5

# -- Graphile Worker (background email jobs; uses DATABASE_URL) ----------------
GRAPHILE_WORKER_ENABLED=true
GRAPHILE_WORKER_CONCURRENCY=5
GRAPHILE_WORKER_POLL_INTERVAL=2000
GRAPHILE_WORKER_SCHEMA=graphile_worker

# -- Outbox --------------------------------------------------------------------
OUTBOX_ENABLED=true
OUTBOX_POLL_INTERVAL_MS=5000
OUTBOX_BATCH_SIZE=20
OUTBOX_MAX_ATTEMPTS=10
OUTBOX_STALE_CLAIM_THRESHOLD_MS=60000
OUTBOX_DEGRADED_PENDING_THRESHOLD=50
OUTBOX_DEGRADED_FAILED_THRESHOLD=1

# -- gRPC server ---------------------------------------------------------------
GRPC_ENABLED=true
GRPC_URL=0.0.0.0:50051

# -- Redis ---------------------------------------------------------------------
REDIS_URL=
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_USERNAME=
REDIS_PASSWORD=                      # VAULT-MANAGED
REDIS_KEY_PREFIX=auth:

# -- Tracing -------------------------------------------------------------------
TRACING_ENABLED=false
OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318/v1/traces
JAEGER_AGENT_HOST=jaeger
OTEL_LOG_LEVEL=

# -- Metrics -------------------------------------------------------------------
METRICS_AUTH_TOKEN=                  # VAULT-MANAGED (để trống = không yêu cầu auth ở local)
