apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: postgres-daily
  namespace: collabspace
spec:
  schedule: "0 2 * * *"
  cluster:
    name: postgres
  backupOwnerReference: self
