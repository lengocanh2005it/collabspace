# ================================================
# Traefik IngressRoute — External client routing
# Infrastructure Engineer: Phan Phu Tho
# ================================================

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: collabspace-routes
  namespace: collabspace
  labels:
    app.kubernetes.io/part-of: collabspace
spec:
  entryPoints:
    - web
    - websecure
  routes:
    # Auth Service — protected self-lookup endpoint
    - match: Path(`/api/v1/auth/me`)
      kind: Rule
      priority: 100
      services:
        - name: auth-service
          port: 3000
      middlewares:
        - name: forward-auth
        - name: rate-limit
        - name: cors-headers

    # Auth Service — /api/v1/auth/*
    - match: PathPrefix(`/api/v1/auth`)
      kind: Rule
      services:
        - name: auth-service
          port: 3000
      middlewares:
        - name: rate-limit
        - name: cors-headers

    # User Service — /api/v1/users/*
    - match: PathPrefix(`/api/v1/users`)
      kind: Rule
      services:
        - name: user-service
          port: 3000
      middlewares:
        - name: forward-auth
        - name: rate-limit
        - name: cors-headers

    # Workspace Service — /api/v1/workspaces/*
    - match: PathPrefix(`/api/v1/workspaces`)
      kind: Rule
      services:
        - name: workspace-service
          port: 8080
      middlewares:
        - name: forward-auth
        - name: rate-limit
        - name: cors-headers

    # Task Service — /api/v1/tasks/*
    - match: PathPrefix(`/api/v1/tasks`)
      kind: Rule
      services:
        - name: task-service
          port: 3000
      middlewares:
        - name: forward-auth
        - name: rate-limit
        - name: cors-headers

    # Notification Service — /api/v1/notifications/*
    - match: PathPrefix(`/api/v1/notifications`)
      kind: Rule
      services:
        - name: notification-service
          port: 3000
      middlewares:
        - name: forward-auth
        - name: rate-limit
        - name: cors-headers

---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: forward-auth
  namespace: collabspace
spec:
  forwardAuth:
    address: http://auth-service.collabspace.svc.cluster.local:3000/api/v1/auth/verify
    trustForwardHeader: true
    authRequestHeaders:
      - Authorization
      - Cookie
      - X-Request-Id
    authResponseHeaders:
      - X-User-Id
      - X-Role
      - X-Workspace-Id
      - X-Request-Id

---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rate-limit
  namespace: collabspace
spec:
  rateLimit:
    average: 100
    burst: 200

---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: cors-headers
  namespace: collabspace
spec:
  headers:
    accessControlAllowMethods:
      - GET
      - POST
      - PUT
      - PATCH
      - DELETE
      - OPTIONS
    accessControlAllowOriginList:
      - "*"
    accessControlAllowHeaders:
      - Content-Type
      - Authorization
    accessControlMaxAge: 86400
