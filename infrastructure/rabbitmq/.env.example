# RabbitMQ Environment Variables
# NOTE: definitions.json is the source of truth for credentials.
# The entrypoint.sh imports definitions.json AFTER creating env-based user,
# so these values only matter if definitions.json is removed.
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest
RABBITMQ_DEFAULT_VHOST=collabspace